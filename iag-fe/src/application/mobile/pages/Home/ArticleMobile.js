import React from "react";

const ArticleMobile = (props) => {
    const [article] = props;

    return (
        <>
        {articles ? articles.map(() => {}) : ''}
        </>
    )
}

export default ArticleMobile